version: "3.9"

x-app: &app
  env_file:
    - ./.env
  restart: always

x-default-logging: &default-logging
  options:
    max-size: '10m'
    max-file: '3'
  driver: json-file

services:
  source-db:
    image: postgres:13
    container_name: source-db
    <<: *app
    logging: *default-logging
    ports:
      - "5432:5432"
    environment:
      TZ: "Europe/Moscow"
      PGTZ: "Europe/Moscow"
    volumes:
      - source_db_data:/var/lib/postgresql/data/
      - ./init_scripts/init_source_db.sql:/docker-entrypoint-initdb.d/init.sql
#      - ./init_scripts/db_dumps/film_work_dump.sql:/docker-entrypoint-initdb.d/film_work_dump.sql
#      - ./init_scripts/db_dumps/genre_dump.sql:/docker-entrypoint-initdb.d/genre_dump.sql
#      - ./init_scripts/db_dumps/genre_film_work_dump.sql:/docker-entrypoint-initdb.d/genre_film_work_dump.sql
#      - ./init_scripts/db_dumps/person_dump.sql:/docker-entrypoint-initdb.d/person_dump.sql
#      - ./init_scripts/db_dumps/person_film_work_dump.sql:/docker-entrypoint-initdb.d/person_film_work_dump.sql
  target-db:
    image: elasticsearch:8.6.2
    container_name: target-db
    <<: *app
    logging: *default-logging
    ports:
      - "9200:9200"
    environment:
      - discovery.type="single-node"
      - xpack.security.enabled=false
    volumes:
      - target_db_data:/usr/share/elasticsearch/data
#  etl:
#    container_name: etl
#    <<: *app
#    logging: *default-logging
#    build: etl


volumes:
  source_db_data:
  target_db_data:
